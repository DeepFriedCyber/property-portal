{
  "name": "property-portal",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "db:migrate": "tsx apps/api/lib/migrate.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.4",
    "turbo": "^1.10.0"
  }
}
